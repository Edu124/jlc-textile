# Copy to .env for local overrides, or set these as Railway variables.

# Provided automatically by Railway's PostgreSQL plugin.
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Shop login (CHANGE THESE before going live)
SHOP_USERNAME=jailaxmi
SHOP_PASSWORD=change-this-password

# Sign JWT tokens — use a long random string in production.
SECRET_KEY=replace-with-a-long-random-secret

# Optional: Stability AI key for Image -> Image generation.
STABILITY_API_KEY=
